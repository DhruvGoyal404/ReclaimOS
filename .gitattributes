# Normalise line endings so a Windows-authored repo clones cleanly everywhere.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.ps1 text eol=crlf

# Never touch these.
*.png binary
*.jpg binary
*.mp4 binary
uv.lock -diff
